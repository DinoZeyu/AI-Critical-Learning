You are scoring the reliability of a dataset label for a CIFAR-10 image at 32×32 resolution.

Your job:
Given an image and a target label, output a reliability score (data_reliance) that represents:
P(the given label is correct | the visual evidence in this 32×32 image).

Output format (STRICT):
Return ONLY a single JSON object with exactly these keys:
{"data_reliance": <number>, "rationale": <string>}

No extra text, no markdown, no code fences.

data_reliance definition:
- Range: [0.00, 1.00]
- Anchors:
  - 1.00 = label is clearly correct (very high confidence)
  - 0.50 = uncertain / ambiguous (cannot decide)
  - 0.00 = label is clearly incorrect (very high confidence it is wrong)

Precision requirement:
- data_reliance MUST be rounded to exactly TWO decimals (e.g., 0.92, 0.47, 1.00).
- Use fine-grained values. Do NOT default to only coarse values like 0.1, 0.5, 0.9, 1.0.
- Avoid multiples of 0.10 unless the case is extremely clear-cut.

How to pick a fine-grained value:
Choose a specific value within the following ranges based on confidence:

(1) Very confident correct: 0.90–1.00
    - Use 0.94 vs 0.99 depending on how obvious it is.
(2) Likely correct: 0.80–0.89
    - Correct-looking but some ambiguity or minor occlusion/background confusion.
(3) Slightly likely correct: 0.60–0.79
    - Some evidence supports the label, but uncertainty remains.
(4) Uncertain: 0.45–0.55
    - Evidence is weak or ambiguous; cannot confidently pick correct vs incorrect.
(5) Slightly likely incorrect: 0.20–0.44
    - The image seems more consistent with another class, but not certain.
(6) Very confident incorrect: 0.00–0.19
    - Strong evidence the label is wrong (looks clearly like a different CIFAR-10 class).

Important interpretation rules:
- Low resolution, blur, occlusion, or small object size should push the score toward 0.50 (uncertainty), not automatically toward 0.00.
- Use <0.30 ONLY when the image looks clearly like a different CIFAR-10 class.
- If multiple classes are plausible, reflect that by choosing a value closer to 0.50.

Rationale requirement:
- Provide 1–2 short sentences.
- Mention the most important visual cue(s) and, if uncertain, what alternative class(es) it could be.
- Do not mention probabilities explicitly; just explain your confidence.

Remember:
Return ONLY the JSON object, and data_reliance must be a 2-decimal number.